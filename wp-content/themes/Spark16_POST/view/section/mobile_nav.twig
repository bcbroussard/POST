<div id="mobile-menu" class="mlpm_w">
  <nav> 
    <ul>
      {% for item in main.nav.get_items %}
      <li>
        <a href="#">{{item.title}}</a>
        {% if item.get_children %}
          <ul>
            {% for child in item.get_children %}
            <li>
                <a href="{{child.get_link}}">{{child.title}}</a>
                {% if child.get_children %}
                  <ul>
                    {% for child in child.get_children %}
                      <li><a href="{{child.get_link}}">{{child.title}}</a></li>
                    {% endfor %}
                  </ul>
                {% endif %}
            </li>
              {% endfor %}
          </ul>
        {% endif %}
      </li>
      {% endfor %}
    </ul>
  	{# <ul>
      {% for item in main.nav.get_items %}
        <li>
          <a href="#">{{item.title}}</a>
          {% if item.get_children %}
              <ul>
                {% for child in item.get_children %}
                  <li><a href="{{child.get_link}}">{{child.title}}</a></li>
                {% endfor %}
              </ul>
          {% endif %}
        </li>
      {% endfor %}
      <li>
        <div class="sub {{ spark_class }}"> 
          <ul>
          {% for item in sub.nav.get_items %}
            <li class="nav-main-item {{item.classes | join(' ')}}"><a class="nav-main-link" href="{{item.get_link}}">{{item.title}}</a>
                {% if item.get_children %}
                    <ul class="nav-drop">
                      {% for child in item.get_children %}
                        <li class="nav-drop-item"><a href="{{child.get_link}}">{{child.title}}</a></li>
                      {% endfor %}
                    </ul>
                 {% endif %}
            </li>
          {% endfor %}
          </ul>
        </div>          
      </li>
  	</ul> #}
  </nav>
</div>